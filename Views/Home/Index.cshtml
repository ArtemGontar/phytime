@model List<string>
@{
    ViewBag.Title = "Index";
}

<h3>RSS-новости научной периодики по психологии</h3>
<h4>В списке @(Model.Count/2) элемента</h4>
@for (int i = 0; i < Model.Count; i += 2)
{
    @Html.ActionLink(Model[i], "RssFeed", new { url = Model[i+1], page = 1 })
    <br />
}
